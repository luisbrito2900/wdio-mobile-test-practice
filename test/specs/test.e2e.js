const { expect } = require("@wdio/globals");
const LoginPage = require("../pageobjects/example.page");
const SecurePage = require("../pageobjects/secure.page");

describe("Login Test Suite", () => {
  it("Login with valid credentials", async () => {});
});
